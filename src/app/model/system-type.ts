export enum SystemType {
  INFINITE_QUEUE = "inf_queue",
  WITH_REJECT = "reject",
  WITH_QUEUE = "queue",
}

export enum SystemParameters {
  SERVERS_NUM = "serversNum",
  SERVE_TIME = "serveTime",
  LAMBDA = "lambda",
  QUEUE_LENGTH = "queueLength",
}
